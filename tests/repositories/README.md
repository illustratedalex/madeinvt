# Repository Tests

This folder is reserved for repository contract tests.

Planned coverage:
- CRUD contract behavior across mock and Supabase-backed repositories
- Archive semantics and status transitions
- Error handling parity between providers