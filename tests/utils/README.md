# Utility Tests

This folder is reserved for utility and service tests.

Planned coverage:
- Cache TTL behavior (`CacheService`)
- Search index generation and query ranking (`SearchIndexService`)
- Image placeholder normalization (`ImageService`)